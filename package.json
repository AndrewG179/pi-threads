{
  "name": "pi-threads",
  "version": "1.0.0",
  "description": "Thread-based orchestrator extension for pi — dispatch actions to persistent worker threads",
  "keywords": ["pi-package"],
  "pi": {
    "extensions": ["./index.ts"]
  }
}
